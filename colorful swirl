float r = 0;
float s = 0;

void setup() {
  size(700,700);
  
}

void draw() {

  fill(random(255),random(255),random(255));
  if(s<600) {
    translate(width/2,height/2);
    rotate(r);
    rect(r,s,8,8);
    r+=1;
    s+=2;
    println(""+r);
  }
}
