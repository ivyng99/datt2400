void setup(){
  size (600,600);
}

void draw(){
  
background (0);
stroke (255);

for (int x = 0; x < width; x = x + 20){
    line (x,0,x,height);
  }
  
for (int y = 0; y < height; y = y + 20){
    line (0,y,width,y);
}
}
